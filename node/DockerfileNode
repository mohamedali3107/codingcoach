# Utilisez une image Ubuntu de base
FROM ubuntu:latest

# Mise à jour des paquets et installation des dépendances
RUN apt-get update -y 

RUN apt install -y nodejs npm

#RUN  curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg

#RUN NODE_MAJOR=20 
#RUN echo "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_$NODE_MAJOR.x nodistro main" |  tee /etc/apt/sources.list.d/nodesource.list

#RUN apt-get install -y nodejs

# Vérification de l'installation de Node.js et npm
#RUN node -v



WORKDIR /app 
COPY . .


# (Optionnel) Vous pouvez ajouter d'autres commandes pour la configuration de votre environnement ici

# Lorsque vous exécutez un conteneur basé sur cette image, Node.js sera installé
